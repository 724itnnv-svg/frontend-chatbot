# Leave empty when the built frontend is served by the Express server.
# The app will use window.location.origin, so phone installs/opened webviews
# call the same IP/domain that served the frontend.
# For a bundled Capacitor APK, set this to your backend URL before building.
VITE_API_URL=https://chatbot-zhpy.onrender.com
